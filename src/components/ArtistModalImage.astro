---

---

<div id="modalBackdrop" class="fixed inset-0 bg-black bg-opacity-80 hidden items-center justify-center z-50" onClick="this.style.display = 'none';">
  <img
    id="modalImage"
    class="max-w-4xl max-h-[90vh] rounded-lg"
    onClick="event.stopPropagation();"
  />
</div>


<style>
      #modalBackdrop {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(0,0,0,0.7);
        justify-content: center;
        align-items: center;
        z-index: 9999;
      }

      #modalImage {
        max-width: 90vw;
        max-height: 90vh;
        border-radius: 0.5rem;
        box-shadow: 0 0 30px rgba(0,0,0,0.5);
      }
</style>